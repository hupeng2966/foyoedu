spring:
  cloud:
    config:
      name: foyoedu-oauth2-config #需要从github上读取的资源名称，注意没有yml后缀名
      #profile配置是什么就取什么配置dev or test
      profile: dev
      label: master
      uri: http://config.foyoedu.com:3344  #SpringCloudConfig获取的服务地址